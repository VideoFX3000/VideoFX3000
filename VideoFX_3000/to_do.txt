To Do Liste:

noch nicht fertige:
ERL - HighPass, HighPassEffekt jeweils .cpp
- VideoEngine.cpp
	- ggf. nur 1 Video speichern
	- Effektwiedergabe in VideoEngine (leicht umzusetzen)
- Doku
- ggf. Video soll weiterlaufen bis keine verzögerten Frames mehr vorhanden sind
- VideoEngine.cpp: kommentieren (zum Schluss)
- Strukturdiagramm
- Präsentation


ERL - Videobuffer, Delay
ERL - Geisterbilder-Effekt baue
ERL (- Webcam)
ERL - Looper: schneller in den neuen Loop reingehen
ERL (- Looper: Webcamfunktion)
ERL - LooperEffect.h: kommentieren
ERL - LooperEffect.cpp: kommentieren
ERL - Backgroundsubstraction.h: kommentieren
ERL - Backgroundsubstraction.cpp: kommentieren
ERL - MagicCapEffect.h: kommentieren wenn die Klasse fertig ist
ERL - MagicCapEffect.cpp: kommentieren wenn die Klasse fertig ist
ERL - VideoEngine.h: kommentieren (zum Schluss)
ERL - VideoEngineMain.cpp: Instanzvariablen der Subklassen und Aufrufe

WICHTIG
ERL - Aufnahmefunktion wieder einbauen
ERL - Konsolensteuerung anpassen (bei "q" außerhalb des Effekts Programm beenden)
ERL - Enter-Aufforderung teilweise entfernen (prüfen wo)
ERL - VideoEngine.cpp: Array statt VideoWriterObjekt
ERL - DelayEffekt-Klasse: in 2 Klassen (DelayEffect/GhostEffect) aufteilen
ERL - MagicCapEffekt-Klasse: in 2 Klassen (DelayEffect/GhostEffect) aufteilen



**************Fragen mit Plaß klären^^***************************

Fragen an Plaß:
ERL - VideoRingBuffer: 
		- wie binden wir die process()-Methode ein? Ein einfacher Aufruf beispielsweise in DelayEffect.cpp funktioniert nicht, da an die einzelnen Methoden Bedingungen geknüpft sind
		- wie verknüpfen wir VideoRingBuffer mit DelayEffect am sinnvollsten, z. B. Implementation einer Methode, getInputParameters?
ERL - Effect:
		- wo binden wir die Metode setTool() am Besten ein?
ERL - VideoEngine.h:
		- hier evtl. Variable vom Typ ToolInterface deklarieren?
ERL - VideoEngine.cpp:
		- wie bekommen wir die Switch-Anweisungen funktionsfähig
ERL - LooperEffect:
		- wie bekommen wir es hin, dass der Looper sofort nach Drücken der Taste "q" beendet wird, ABER der looper so wie bisher funktioniert (also anschließend weitere Loops aufgezeichnet werden können)
- DelayEffect:
ERL	- ist es sinnvoll, die Verarbeitungen für ein gewisses Intervall durchzuführen, also z. B. pro Sekunde? (wie würde das bei eine FrameRate von 29.97 funktionieren?)
OFFEN	- wie realisieren wir, dass der DelayEffect immer bis zum Ende läuft (beispielsweise bricht die Wiedergabe ab, wenn das video zu Ende ist). Am Besten wäre es, wenn die verzögerten Frames zum Ende hin ausgeblendet
	  würden, sodass das Video schließlich mit einem Frame endet.